[build]
  base = "server"
  command = "npm install && npm run build"
  publish = "server/dist"
  functions = "functions"

[[redirects]]
  from = "/api/*"
  to = "/.netlify/functions/server"
  status = 200 